# 자기소개서 초안 — AI 활용 · 문제 해결 (3문항)

> **초안이다. 문장을 소유하는 것은 본인이다.** 아래 내용은 전부 실제 프로젝트 문서(`src/assets/**`, `src/content/**`)에서
> 확인한 사실로만 채웠다. 지어낸 수치·경험은 없다.
> `⚠ 확인` 표시가 붙은 곳은 **본인이 최종 확인·수정해야 하는 자리**다(주로 정량 수치와 개인적 판단).

---

## 1) AI를 활용해 생산성을 높인 경험 — 사용한 도구 · 해결한 문제 · 개선된 결과

**4인 팀 프로젝트 'Bond'(로그라이크 턴제 RPG)에서 저는 AI 코딩 에이전트를 단순 자동완성이 아니라 Plan · Build · Review 세 역할로 나눈 개발 사이클로 운용했습니다.**

제가 담당한 부분은 원정(맵 진행)·이벤트·전투 연출을 아우르는 서브시스템으로, **21개 모듈 · 약 100개의 스크립트** 규모를 혼자 설계·구현해야 했습니다. 규모가 커질수록 혼자서는 모든 코드 경로의 결함을 사전에 잡기 어렵다는 것이 문제였습니다.

그래서 구현 단계마다 **Review 에이전트의 정적 분석을 코드 리뷰에 붙였고, 게임을 실행하거나 테스트를 돌리기 전에 결함 3건을 미리 제거**했습니다.

- **중복 호출 시 오브젝트가 누적되던 문제** — 특정 초기화가 두 번 불리면 인스턴스가 쌓이는 경로
- **배열 길이 불일치로 인한 예외** — 인덱스 접근이 런타임에 터질 수 있던 지점
- **씬 이중 로드 방지 로직 누락** — 같은 씬이 겹쳐 로드될 수 있던 가드 부재

**개선된 결과:** 세 결함은 모두 런타임에서야 드러났을 버그였는데, 정적 분석 단계에서 걸러내 재현·디버깅 비용을 아꼈습니다. 그 위에 CI/CD 빌드 자동화까지 붙여, 1인이 담당한 대규모 서브시스템을 안정적으로 완성할 수 있었습니다.

또한 개인 프로젝트로는, **흩어져 있던 여섯 개의 Unity 프로젝트 소스 코드를 AI(Claude Code)로 읽어 기술 문서 웹사이트로 변환하는 빌드 파이프라인**을 직접 구축했습니다. 손으로 PDF 포트폴리오를 만들던 반복 작업을 없애고, "새 프로젝트를 추가하면 사이트가 갱신되는" 구조로 정리한 경험입니다.

> ⚠ 확인 — 정량 지표를 한 줄 넣으면 설득력이 커집니다. Bond의 실측(GC 할당/드로우콜 등)이 있으면 여기에 추가하세요.
> (참고: 뱀서라이크 프로젝트에는 `GC Alloc 196회 → 4회(97% 감소)`, `Materials 824회 → 249회(70% 감소)`, `메모리 140MB 절감` 같은
> 실측 수치가 있습니다. 이 문항이 AI 활용이 아니라 최적화 경험을 물었다면 그 프로젝트로 바꾸는 편이 낫습니다.)

---

## 2) AI 활용 능력

**저는 AI를 '코드를 대신 써주는 도구'가 아니라 '판단의 속도를 높여주는 도구'로 봅니다. 무엇을 어떻게 만들지 설계하고, AI가 내놓은 결과를 검증하고 책임지는 일은 개발자의 몫이라고 생각합니다.** 이 관점을 세 가지 방식으로 실천하고 있습니다.

**첫째, AI의 제안을 그대로 수용하지 않고 검증한 뒤 필요하면 뒤집습니다.** Bond에서 전투 도중 컷씬이 뜨면 컷씬의 완료 콜백이 전투 콜백을 덮어써, 컷씬이 끝난 뒤 맵으로 복귀하지 못하는 버그가 있었습니다. AI는 '컷씬 종료 후 콜백을 다시 등록하는 방식'과 '컷씬 전용 채널을 새로 만드는 방식'을 제안했지만, 전자는 되돌리는 로직이 호출부마다 흩어져 중첩이 깊어질수록 복잡해지고 후자는 씬 종류가 늘 때마다 채널이 증식하는 문제가 있었습니다. 저는 두 제안을 받지 않고 **콜백 슬롯을 스택(Stack) 구조로 바꿔 가장 최근 씬부터 꺼내도록** 설계했습니다. 씬이 더 깊게 중첩돼도 그대로 동작하고 기존 코드 수정도 최소였습니다.

**둘째, AI를 리뷰어로 활용하되 '무엇이 결함인가'의 기준은 제가 소유합니다.** 위 1번의 정적 분석 리뷰가 그 예로, AI는 의심 지점을 넓게 훑어주지만 그것이 실제 결함인지, 어떤 우선순위로 고칠지는 코드 맥락을 아는 사람이 판단해야 한다고 봅니다.

**셋째, AI가 생성하더라도 품질은 구조로 강제합니다.** 포트폴리오 파이프라인에서는 필수 항목(측정 근거·의사결정 등)이 빠지면 아예 빌드가 실패하도록 스키마를 품질 게이트로 설계했습니다. 사람의 성실함이 아니라 구조로 일정한 품질을 보장하는 방식입니다.

정리하면, **AI는 생산성을 크게 끌어올리는 도구지만 설계와 최종 판단은 개발자가 쥐고 있어야 한다**는 것이 저의 활용 원칙입니다.

> ⚠ 확인 — 2)의 축은 본인 자기소개서 '장기 목표'(AI가 발전할수록 코드 작성보다 설계·판단 역량이 중요)와 일치시켰습니다. 그대로 두어도 되고, 톤을 더 겸손하게/강하게 조절하셔도 됩니다.

---

## 3) 직접 해결한 가장 어려운 개발 문제 — 문제 상황 · 원인 분석 · 해결 · 결과

**대상:** 1인 개발 3인칭 소울라이크 프로젝트의 몬스터 AI (행동 트리, Behaviour Tree)

**① 문제 상황** — 몬스터가 연속 공격(콤보)을 하던 도중 플레이어가 공격 사거리 밖으로 빠져나가면, 진행 중이던 콤보가 즉시 끊기고 첫 동작부터 다시 시작됐습니다. 전투가 뚝뚝 끊겨 몬스터의 행동이 어색하게 보였습니다.

**② 원인 분석** — 행동 트리의 기본 Sequence(순차) 노드는 **매 프레임 첫 자식부터 다시 평가**한다는 점이 원인이었습니다. 콤보 시퀀스의 앞쪽에 '사거리 안인가'를 검사하는 조건이 있었는데, 콤보 도중 플레이어가 사거리를 벗어나 이 조건이 한 번이라도 실패(Failure)하면 시퀀스 전체가 리셋되고, 이미 재생 중이던 공격 노드까지 중단됐습니다. 즉 **노드가 '진행 위치'를 기억하지 못하는 자료구조 특성**이 근본 원인이었습니다.

**③ 해결** — 실행 위치를 기억하는 **Sequence_Memory 노드**를 직접 만들었습니다. 어떤 자식이 Running(실행 중) 상태이면 그 인덱스를 저장해 두었다가, 다음 프레임에 처음이 아니라 **그 자리에서 재개**하도록 했습니다. 이렇게 하면 진입 조건을 다시 통과하지 않아도 진행 중이던 타부터 이어져 콤보가 완성됩니다. 그리고 **중단되면 안 되는 연속 행동은 Sequence_Memory, 순수 조건 검사만 필요한 곳은 기존 Sequence**로 역할을 나눠, 같은 실수가 다른 곳에서 반복되지 않도록 구조를 정리했습니다.

**④ 결과** — 콤보가 중간에 끊기지 않고 끝까지 이어졌습니다. 나아가 **같은 계열의 버그**도 함께 잡았습니다. 정지 상태인데도 이동 파라미터가 1로 남아 걷기 모션이 계속 재생되던 문제였는데, 애니메이션과 이동 값을 0으로 되돌리고 항상 성공을 반환하는 **Cleaner 노드를 시퀀스 중간에 삽입**해 해결했습니다.

이 경험에서 얻은 가장 큰 배움은, **비선형 자료구조(트리)를 다룰 때는 각 노드가 상태를 어떻게 유지·전이하는지를 먼저 도식으로 이해해야 한다**는 것이었습니다. 눈에 보이는 증상(콤보 끊김·모션 잔류)을 하나씩 고치는 대신, "노드가 진행 상태를 잃는다"는 공통 원인을 찾아 구조로 정리했다는 점이 스스로 만족스러웠습니다.

> ⚠ 확인 — 3)의 소재는 아직 다른 문항에 쓰이지 않아 신선한 'SoulLike 콤보 끊김'으로 골랐습니다.
> 본인이 더 어렵게 느낀 문제가 있으면 아래 **대안 소재**로 교체하세요.

### 3번 문항 대안 소재 (원하시면 교체)

**대안 A — 버민타이드 프레임 드롭 (원인 분석·수치 결과가 가장 강력)**
- 문제: 원작처럼 실내 맵을 탐험하며 웨이브를 상대하는 구조인데, 복잡한 미로형 실내에서 프레임이 15FPS 이하로 떨어짐.
- 원인 분석: Unity 프로파일러를 집중 학습해 `Gfx.WaitForPresent` 마커(GPU 작업 완료 대기)를 확인 → GPU 병목으로 판단. 가려지는 오브젝트가 많은 미로 구조가 렌더링 비용을 키운다고 봄.
- 해결: 오클루전 컬링(Occlusion Culling) 적용으로 보이지 않는 오브젝트의 렌더링 제거.
- 결과: **15FPS 이하 → 60FPS 수준으로 안정화.** (※ 이미 '성장 과정' 자소서에 쓰여 있으니, 같은 회사 제출이면 중복 주의)

**대안 B — 기업 협약 프로젝트, 빌드에서만 나던 null 참조**
- 문제: 에디터에선 멀쩡한데 **빌드 단계에서만** 매니저 클래스가 null로 참조되는 오류.
- 원인 분석: Logcat으로 추적 → 빌드 환경의 스크립트 실행 순서가 에디터와 달라져 발생. 근본 원인은 **클래스 간 결합도가 높아 특정 실행 순서에 의존하는 구조**였음.
- 해결: 실행 순서 의존을 없애기 위해 인터페이스 의존 + DI(VContainer) 도입을 팀에 제안·전환.
- 결과: 초기화 순서에 기대지 않는 구조로 개선. (※ 이미 '팀프로젝트 경험' 자소서에 쓰여 있음)

---

## 근거 출처 (지어내지 않았음을 확인용)

| 문항 | 사실 근거 파일 |
| --- | --- |
| 1) AI 에이전트 · 결함 3건 · 규모 | `src/assets/bond/2-판단-Bond.md` (satisfaction·evidence), `src/assets/bond/98_LeeJuno_1-구현.md` |
| 1) 파이프라인 개인 프로젝트 | 이 저장소 `CLAUDE.md` |
| 2) AI 제안을 뒤집은 역제안 | `src/assets/bond/2-판단-Bond.md` (결정 ③ StageCompletionChannel) |
| 3) 콤보 끊김 · Cleaner | `src/assets/soulslike/26_0720_SoulLike_구현-AI.md` (기능 ③, Sequence_Memory·Leaf_Cleaner) |
| 대안 A 버민타이드 | `src/content/about/about.yaml` (성장 과정) |
| 대안 B 기업협약 | `src/content/about/about.yaml` (팀프로젝트 경험) |
