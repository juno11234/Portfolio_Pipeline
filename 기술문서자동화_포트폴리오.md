# 기술 문서 자동화 파이프라인 (Portfolio_Pipeline)

> Unity/C# 프로젝트의 **소스 코드를 입력받아 기술 문서 웹사이트로 변환**하는 정적 사이트 빌드 파이프라인.
> 손으로 PDF 포트폴리오를 만들던 반복 작업을 없애고, 일정한 품질을 *구조로* 강제했다.

| | |
| --- | --- |
| **역할** | 1인 (기획·설계·구현 전부) |
| **기간** | 2026.07.16 ~ 07.29 |
| **분류** | 개발 생산성 도구 · 정적 사이트 생성 |
| **스택** | Astro · TypeScript · Content Collections + Zod · Mermaid · Shiki · GitHub Actions |
| **결과** | 6개 프로젝트 기술 문서를 코드에서 생성·배포 |
| **라이브** | https://juno11234.github.io/Portfolio_Pipeline/ |

---

## 왜 만들었나

기존에는 프로젝트마다 손으로 PDF 기술 문서를 만들었습니다. 반복적인 데다, 프로젝트가 늘 때마다 문서의 품질이 들쭉날쭉했습니다. **"코드 → 사이트"를 자동으로 잇고, 문서 품질을 사람의 성실함이 아니라 구조로 보장**하는 것을 목표로 삼았습니다.

## 무엇을 만들었나

프로젝트별 기술 문서를 **데이터(YAML)로 정의**하면, 파이프라인이 이를 검증해 **정적 HTML로 조립**합니다. 새 프로젝트를 추가할 때 파이프라인 코드는 건드리지 않고 데이터 파일 하나만 추가하면 됩니다. 실제로 이 방식으로 **6개 프로젝트**(뱀서라이크·Bond·소울라이크·스타워즈·기업협약·버민타이드)의 기술 문서를 전부 생성했습니다.

## 핵심 설계 원칙

**1. 3층 분리** — 데이터(콘텐츠) · 템플릿(디자인) · 빌드를 분리했습니다. 프로젝트를 추가할 때 템플릿을 건드리지 않고, 디자인을 바꿀 때 콘텐츠를 건드리지 않습니다.

**2. 스키마가 품질 게이트** — 필수 항목(측정 근거·의사결정·회고 등)이 빠진 문서는 **빌드가 깨져** 배포되지 않습니다. "아쉬운 점 없습니다" 같은 빈 문서를 구조적으로 차단했습니다.

**3. 프로젝트 추가 = 파일 1개 추가** — 프로젝트가 10개로 늘어도 파이프라인 코드는 그대로입니다. 확장성을 구조로 보장했습니다. (6번째 문서를 추가할 때 새 코드·스키마 변경이 0이었던 것으로 검증)

## 직접 구현한 것

- **빌드 시점 렌더링** — Mermaid 다이어그램과 C# 코드 하이라이팅을 빌드 때 미리 SVG/HTML로 구워, **브라우저로 나가는 JS를 0으로** 만들었습니다. 렌더러는 싱글턴+캐시로 중복 실행을 막았습니다.
- **자동 레이아웃 규칙** — 카드 묶음을 본문 길이(60자 기준)에 따라 가로 격자↔세로 스택으로 렌더러가 자동 결정합니다. 콘텐츠 파일이 CSS를 들지 않도록 했습니다.
- **문장 강조 파서** — 문장 안에서 `**굵게**` · `==해법==`(파랑) · `!!실패!!`(빨강)만 여는 최소 마크업을 직접 구현. 표·목록 같은 구조가 문장에 숨어들지 않게 강조만 허용했습니다.
- **디자인 → 컴포넌트 복제** — 디자인 원본의 마크업을 눈대중이 아니라 그대로 떠서 **50여 개 컴포넌트**로 만들었습니다. 인라인 스타일 700여 개 중 81%가 반복이라 컴포넌트화가 성립한다는 것을 측정으로 확인했습니다.

## 배포

`main` 브랜치에 푸시하면 GitHub Actions가 빌드해 GitHub Pages로 자동 배포합니다. 결과물이 순수 정적 HTML이라 무료 호스팅이 가능합니다.

## 어필 포인트

- 기능 구현을 넘어 **반복 작업을 자동화하는 도구를 직접 설계**한 경험
- **AI(Claude Code)를 활용**해 게임 프로젝트 소스에서 기술 문서를 생성하는 워크플로를 구축
- 게임 클라이언트 외에 **TypeScript · 정적 사이트 · CI/CD**까지 다룬 경험
- 품질·확장성을 *구조(스키마·계층 분리)*로 보장하는 소프트웨어 설계 관점

> ⚠ 확인 — 위 수치·링크는 저장소 `CLAUDE.md`에서 확인한 사실입니다. 포트폴리오에 넣기 전
> 라이브 링크가 유효한지, 회사 제출용으로 스크린샷/GIF를 1~2장 곁들일지 정하시면 됩니다.
