# 기술문서 양식 ③ — 자산 인계 (스크린샷·링크: 사람이 넣는다)

> **채우는 주체:** 사람. AI 가 코드에서 만들 수 없는 **파일 · 주소**를 넘긴다.
>   (구현은 양식 ①, 판단 · 회고 텍스트는 양식 ②.)
> **쓰는 법:** 이 파일을 `src/assets/{slug}/captions.md` 로 복사 → 그 폴더에 png 를 넣고 → 아래 표를 채운다.
>   이미지와 표가 **같은 폴더**에 있어 짝이 어긋나지 않는다.
> **캡션은 비워도 된다.** 비우면 AI 가 그 이미지를 열어 보고 초안을 쓴다(사용자가 확인 · 수정).

## 스크린샷
`src/assets/{slug}/` 에 png 를 넣고 한 줄씩 채운다.
- **위치**: `hero`(개요 대표 화면, **1장 필수**) · `기능: {기능 제목}`(본문 이미지) · `회고`
- **캡션**: 무엇을 보라는 건지. 없으면 장식이다. (비우면 AI 가 이미지 보고 초안)

| 파일명 | 위치        | 캡션(무엇을 보라는 건지)            |
|---|-----------|---------------------------|
| «bond-gameplay.png» | hero      | «»                        |
| «bond-map» | «map»     | «생성된 맵노드»                 |
| «bond-silhouette» | «커스텀 셰이더» | «원본↔단색 연속 보간 실루엣 셰이더»     |
| «bond-speedline» | «커스텀 셰이더» | «텍스처 없는 속도선 생성»           |
| «mapPipeline» | «map»     | «맵생성 파이프라인»               |
| «ScopeTest» | «결과, 회고»  | «스코프 상속구조 직접 테스트 해봤다는 증거» |


## 링크 (있으면)
- 튜토리얼 유튜브: «https://youtu.be/pl6heL8qC0A?si=a3VGrK8iV_CCRR1u»
- - 전투 유튜브: «https://youtu.be/OQSDaqbAk8Y?si=tUPoFDoM9UaqLQ2z»
- 깃허브: «https://github.com/Dost-Released-Project/Bond.git»
> 없으면 비운다. 확인 안 된 주소를 지어내지 않는다.

## 확인만 필요한 메타 (AI 가 코드로 못 얻는 것)
- **team**(인원): «4인 팀»
- **vcs**(형상관리): « SourceTree»
- **ide**: «Rider 2025.2.1»
> 기간 · 엔진 버전은 AI 가 `git log` · `ProjectSettings/ProjectVersion.txt` 에서 뽑는다 — 맞는지 **확인만**.

---
채우고 나면 양식 ① · ② 와 함께 AI 에게 넘겨 `src/content/docs/{slug}.yaml` 로 조립시킨다.
